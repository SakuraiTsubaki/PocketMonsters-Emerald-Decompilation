# Thumb bootstrap call targets

The established Japanese target's entry CFG contains 29 direct BL sites and 27 unique in-ROM targets. Target `0x080004c4` is called three times; all other targets occur once. These addresses are reconstruction candidates, not inferred function names. Callee traversal remains a separate verified step.
